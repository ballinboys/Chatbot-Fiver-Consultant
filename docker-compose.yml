version: "3.9"

services:
  api:
    build: .
    container_name: alliance-osteo-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
    restart: unless-stopped
